/** sessionStorage key used to hand the generated analysis from the form to the
 * /result page (no server session needed — access is gated at /api/analyze). */
export const RESULT_STORAGE_KEY = "negotiateai:result";
